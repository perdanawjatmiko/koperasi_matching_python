# Referensi default

Letakkan workbook referensi produksi dengan nama:

`referensi_koperasi.xlsx`

Command `koperasi-match match` menggunakan file tersebut secara default. File referensi lain dapat dipilih dengan opsi `--reference`.
